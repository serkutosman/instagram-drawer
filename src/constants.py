from sys import platform

# FLAGS
VERBOSE_FLAG = "-v"
VERBOSE_FLAG_LONG = "--verbose"

# PATHS
CREDENTIALS_FILE = "credentials.txt"

# COMMANDS
INSTALL_REQUIREMENTS_UNIX = "pip3 install -r requirements.txt"
INSTALL_REQUIREMENTS_WIN = "pip install -r requirements.txt"
